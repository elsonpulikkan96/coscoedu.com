# PUBLIC config baked into the production build (staging + prod).
# This is the PUBLISHABLE (anon) key — safe to expose: it is shipped in the
# browser bundle regardless, and Row Level Security means the public can only
# INSERT form submissions, never read data. Do NOT put the secret key here.
VITE_SUPABASE_URL=https://trubcyitreqrvkqmumen.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_7Rt8kA0LLF5GhXOgcvluqg_QV9B5jpN
